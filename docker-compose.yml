services:
  frontend:
    build:
      context: ./
      # platform: linux/amd64  # Force x64 architecture

    restart: always
    working_dir: /app
    ports:
      - "3001:3001"
    volumes:
      - ./:/app
